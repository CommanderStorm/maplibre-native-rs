[package]
name = "tile-server"
version = "0.0.0"
publish = false
edition.workspace = true

[dependencies]
axum.workspace = true
image.workspace = true
maplibre_native = { workspace = true, features = ["pool"] }
moka = { version = "0.12", features = ["future"] }
thiserror.workspace = true
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
